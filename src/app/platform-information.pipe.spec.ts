import { PlatformInformationPipe } from './platform-information.pipe';

describe('PlatformInformationPipe', () => {
  it('create an instance', () => {
    const pipe = new PlatformInformationPipe();
    expect(pipe).toBeTruthy();
  });
});
